// Like Model
// Handles creation of like objects and optional in-memory operations

export default class LikeModel {
    constructor(userId, postId) {
        this.userId = userId;
        this.postId = postId;
    }
}
